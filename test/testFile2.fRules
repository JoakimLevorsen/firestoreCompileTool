interface A {
    foo: Number;
    bar: String | Timestamp;
    name: String;
    age: Number | Timestamp | String;
}

interface B {
    value: Bool;
}

match /collection/[item] {
    read, delete: {
        if item.a == true {
            return item only B;
        }
        if item is B {
            return true
        }
        return item is A;
    },
    write: resource only A;
}

match /collection/[things]/subCollection/[othertings] {
    read: resource is A;
}