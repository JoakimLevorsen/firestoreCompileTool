interface A {
    foo: Number;
    bar: String | Timestamp;
}

match /collection/[item] {
    read, delete: {
        if (item.a == true) {
            return item is A;
        }
        return item is A;
    },
    write: item is A;
}