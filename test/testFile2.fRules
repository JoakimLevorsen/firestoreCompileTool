interface A {
    foo: Number;
    bar: String | Timestamp;
}

interface B {
    value: Bool;
}

match /collection/[item] {
    read, delete: {
        if item.a == true {
            return item is B;
        }
        return item is A;
    },
    write: item is A;
}